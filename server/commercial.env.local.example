# Sao chép thành commercial.env.local (đã gitignore).
# Lấy từ Supabase → Project Settings → API (cùng project với SUPABASE_URL):
#   SUPABASE_JWT_SECRET = mục "JWT Secret" (Legacy / HS256 — dùng verify access token desktop)
#   SUPABASE_ANON_KEY   = mục "anon" "public" key

SUPABASE_JWT_SECRET=
SUPABASE_ANON_KEY=
